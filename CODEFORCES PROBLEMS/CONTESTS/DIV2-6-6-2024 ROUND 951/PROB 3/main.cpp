#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;


vector<int> inputs(int num){
    vector<int> inp;
    int x;
    for (int i = 0; i < num; i ++){
        cin >> x;
        inp.push_back(x);
    }
    return inp;
}


int main() {

    int numTries;
    cin >> numTries;

    for (int i = 0; i < numTries; i++){
        int num;
        cin >> num;
        vector<int> = inputs(num);
        for (int i = 0; i < inputs.size(); i ++) {

        }

    }



    return 0;
}
